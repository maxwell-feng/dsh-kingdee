# DSH peer packages are installed explicitly at the version this plugin was
# verified against (see package.json). Keep pnpm from auto-installing a second
# copy of them here, which would silently escape that pin.
auto-install-peers=false
