# Dependencies
node_modules/
.pnpm-store/

# Build output — never tracked: the repository holds TypeScript sources only.
# `pnpm build` (and npm's `prepublishOnly` hook) emit lib/ before packing.
lib/
*.tsbuildinfo
*.tgz

# Test / coverage
coverage/
.mwc/
.nyc_output/

# Local config / secrets
.env
.env.*
!.env.example
*.local
*.secret
cordis.local.yml

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
.mimosa/
